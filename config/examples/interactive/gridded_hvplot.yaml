inputs:
   - name: https://psl.noaa.gov/thredds/dodsC/Datasets/ncep.reanalysis.derived/surface/air.mon.mean.nc
     description: Data from NCEP initialized reanalysis
     to_plot:
        air: xt,xy

outputs:
    print_to_file: yes
    output_dir: ${HOME}/scratch/eviz/output/gridded_hvplot
    visualization:
        backend: hvplot
