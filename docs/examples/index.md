# Examples

This section provides hands-on examples of using EViz to create various types of visualizations. Each example includes the command-line syntax, expected outputs, and explanations of the generated plots.

## Prerequisites

Before running these examples, ensure you have:
- EViz installed and configured
- The `EVIZ_CONFIG_PATH` environment variable set. For  these examples set `EVIZ_CONFIG_PATH=config/examples`.
- Sample data files in the expected locations

## Example Categories

```{toctree}
:maxdepth: 1

gridded_data
observational_data
model_specific
```

## Quick Reference

For a complete list of command options:
```bash
python autoviz.py --help
```

For configuration file templates used in this section, see the `config/examples/` directory in your EViz installation.